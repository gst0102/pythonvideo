# 2026-06-07 008迁移与结算专项验证记录

## 目标

执行阶段二 `008_stage2_game_settlement` 数据库迁移，并在目标环境验证小游戏广告与每日结算专项脚本。

## 执行环境

- 仓库路径：`/opt/pythonvideo`
- 分支：`feature/yuexiang-stage2-mvp`
- 代码提交：`a386497 test: add game settlement verification`
- 数据库迁移前版本：`007_stage2_points_foundation`
- 数据库迁移后版本：`008_stage2_game_settlement`

## 已执行动作

1. 生产后端仓库执行 `git fetch` 与 fast-forward，更新到 `a386497`。
2. 使用已拉取的仓库代码目录作为一次性容器挂载源，执行 Alembic 迁移。
3. 执行结算专项脚本：
   - `python scripts/verify_game_settlement_flow.py --execute`
4. 执行小游戏广告专项脚本：
   - `python scripts/verify_game_ad_flow.py --execute`

## 验证结果

- Alembic 升级成功：`007_stage2_points_foundation -> 008_stage2_game_settlement`
- 当前数据库版本确认：`008_stage2_game_settlement (head)`
- 结算专项验证通过：
  - 首次结算
  - 重跑微调
  - `game_adjust_add / game_adjust_sub`
  - 昨日已结算积分摘要
- 小游戏广告专项验证通过：
  - 广告位轮换
  - 全部广告位耗尽提示
  - 同一 `ad_event_id` 幂等
  - 同一局游戏幂等

## 说明

- 两个专项脚本均在事务中执行，结束后回滚测试数据。
- 本次只完成数据库迁移与专项验证；生产运行容器仍需后续使用包含 `a386497` 的正式镜像重建，才能让新增后台接口在运行服务中直接可用。
