# 2026-06-07 生产镜像重建与接口验证记录

## 目标

将生产运行服务切换到包含阶段二结算代码的镜像，并确认新增接口已经被运行容器加载。

## 执行环境

- 生产仓库路径：`/opt/pythonvideo`
- 分支：`feature/yuexiang-stage2-mvp`
- 仓库提交：`53f97b6 docs: record stage2 settlement migration verification`
- 新镜像标签：`pythonvideo-app:stage2-53f97b6`
- 原运行镜像：`pythonvideo-app:stage2-0fa4f8e`

## 执行步骤

1. 基于稳定镜像 `pythonvideo-app:stage2-0fa4f8e` 重建代码层镜像。
2. 将生产 `.env` 的 `APP_IMAGE` 更新为 `pythonvideo-app:stage2-53f97b6`。
3. 执行 `docker compose up -d --no-build app` 重建 app 容器。
4. 等待容器健康检查变为 `healthy`。
5. 执行接口检查：
   - `GET /health`
   - `GET /admin/game-settlements/daily`
   - `GET /points/ledger`

## 验证结果

- app 容器当前镜像：`pythonvideo-app:stage2-53f97b6`
- app 容器状态：`healthy`
- `GET /health` 返回 `200 OK`
- `GET /admin/game-settlements/daily` 返回 `200 OK`
- `GET /points/ledger` 未登录返回 `401 Unauthorized`

## 结论

生产运行容器已加载阶段二结算接口代码；`008_stage2_game_settlement` 迁移后的后台结算入口已可访问。

## 注意事项

- 本次镜像重建复用既有依赖层，仅更新应用代码层；未重新下载 Playwright Chromium。
- 后续若新增依赖或修改运行时系统包，仍需走完整镜像构建链路。
