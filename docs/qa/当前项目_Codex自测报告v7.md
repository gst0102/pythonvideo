# 当前项目 Codex 自测报告 v7

生成日期：2026-06-07

## 1. 本轮结论先说清楚：为什么一直不通过

结论：不通过。

不是因为项目不能编译，也不是因为 PC 后台或小程序构建失败。本轮后端静态编译、PC 后台构建、小程序类型检查、微信小程序构建均已通过。

真正原因是：前几轮 v4、v5、v6 都没有提交 P0 业务修复，只是在复跑编译、构建和归档自测报告。因此只要以下 P0 阻断还存在，按 `AGENTS.md` 和测试清单规则，结论就必须继续是不通过：

1. 会员每日小游戏次数默认值仍未对齐开发文档要求。
2. 四方资产一致性仍缺少目标环境闭环证据。
3. D+2 自动兜底结算仍缺少真实调度级闭环证据。
4. 二级分销收益仍缺少完整验收闭环。
5. 真实广告中断/游戏失败链路、历史配置隔离、历史数据迁移抽样仍未完成验收闭环。

换句话说：构建通过只能证明“代码能打包”，不能证明“积分、收益、广告、分销、提现、后台账务都正确”。当前卡住的是后者。

## 2. 本次自测目标

根据用户要求，本轮作为开发 Codex 继续按测试清单逐条复核当前项目状态，优先验证 P0，并执行已有自动化测试命令。

本轮没有修改核心业务代码，没有修复 Bug 单，仅输出自测报告：

```text
D:\Desktop\vedo-project\myproject\docs\qa\当前项目_Codex自测报告v7.md
```

## 3. 已读取参考材料

| 材料 | 路径 | 读取结果 |
|---|---|---|
| 项目规则 | `D:\Desktop\vedo-project\AGENTS.md` | 已读取 |
| 阶段 2 开发文档 | `D:\Desktop\vedo-project\myproject\docs\yuexiang-stage2-docs\09-ecpm-settlement-rework.md` | 已读取 |
| 测试清单与验收标准 | `D:\Desktop\vedo-project\myproject\docs\qa\当前项目_测试清单与验收标准v1.md` | 已读取 |
| AI 测试官验收报告 | `D:\Desktop\vedo-project\myproject\docs\qa\当前项目_验收报告v4.md` | 已读取 |
| 上一轮 Codex 自测报告 | `D:\Desktop\vedo-project\myproject\docs\qa\当前项目_Codex自测报告v6.md` | 已参考 |

## 4. 修改文件列表

| 文件 | 修改类型 | 说明 |
|---|---|---|
| `D:\Desktop\vedo-project\myproject\docs\qa\当前项目_Codex自测报告v7.md` | 新增 | 记录本轮自测结果、阻断原因和建议修复项 |

本轮没有修改后端、PC 后台、小程序端或数据库核心业务代码。

## 5. 已执行自动化测试命令

| 编号 | 范围 | 命令 | 工作目录 | 结果 |
|---|---|---|---|---|
| T-001 | 后端静态编译 | `python -m compileall controllers services schemas models scripts -q` | `D:\Desktop\vedo-project\myproject` | 通过 |
| T-002 | PC 后台生产构建 | `npm.cmd run build` | `D:\Desktop\vedo-project\adminVideo` | 通过，有构建警告 |
| T-003 | 小程序类型检查 | `npm.cmd run type-check` | `D:\Desktop\vedo-project\video-ts` | 通过 |
| T-004 | 小程序微信构建 | `npm.cmd run build:mp-weixin` | `D:\Desktop\vedo-project\video-ts` | 通过，有依赖提示 |
| T-005 | 会员每日小游戏次数口径抽查 | `rg -n "daily_game_task_limit|VIP_PACKAGE_DAILY_LIMITS|game_limit" ...` | `D:\Desktop\vedo-project` | 发现旧口径仍存在 |

### 构建警告

PC 后台构建通过，但仍有非阻断警告：

- Rollup 对 `#__PURE__` 注释位置发出警告并移除相关注释。
- 部分构建 chunk 超过 500 kB。

小程序微信构建通过，但仍有非阻断提示：

- uni-app 提示存在新版本。
- Sass legacy JS API deprecation warning。

以上警告不构成本轮 P0 阻断，但上线前建议继续纳入技术债和性能检查。

## 6. 已通过测试项

结合现有专项脚本、历史自测报告和本轮自动化命令复跑，本轮确认以下项目有通过证据或主路径验证基础：

| 测试项 | 模块 | 当前状态 | 说明 |
|---|---|---|---|
| TC-004 | 签到固定积分到账 | 已通过 | 签到主链路已有专项验证基础 |
| TC-005 | 当日重复签到拦截 | 已通过 | 重复签到幂等已有验证基础 |
| TC-007 | 小游戏广告完成后生成预估积分 | 已通过主路径 | 主链路已有脚本验证基础 |
| TC-010 | 同一局重复结算防重 | 已通过主路径 | 幂等主路径已有验证基础 |
| TC-011 | 刷新页面不重复发放 | 已通过主路径 | 防刷新重复主路径已有验证基础 |
| TC-014 | 预估积分口径入账 | 已通过主路径 | 预估口径已有专项验证基础 |
| TC-015 | D+1 真实 eCPM 结算 | 已通过主路径 | D+1 主流程已有专项验证基础 |
| TC-019 | 真实数据后补差额调整 | 已通过主路径 | 差额调整主流程已有验证基础 |
| TC-020 | 重复补差幂等 | 已通过主路径 | 补差幂等主流程已有验证基础 |
| TC-021 | 积分流水类型 | 已通过主路径 | 流水类型主路径已有验证基础 |
| TC-026 | 一级邀请收益 | 已通过主路径 | 一级邀请主路径已有验证基础 |
| TC-028 | 禁止自己邀请自己 | 已通过 | 风控拦截已有验证基础 |
| TC-029 | 禁止重复绑定邀请人 | 已通过 | 邀请关系幂等已有验证基础 |
| TC-035 | 仅已结算积分可提现 | 已通过主路径 | 提现口径主路径已有验证基础 |
| TC-036 | 提现冻结/成功/驳回口径 | 已通过主路径 | 提现流转主路径已有验证基础 |
| TC-037 | 会员购买后权益生效 | 部分通过 | 权益主路径有验证基础，但每日小游戏次数口径异常 |
| TC-038 | 重复支付回调幂等 | 已通过主路径 | 支付幂等主路径已有验证基础 |

## 7. 未覆盖测试项

以下项目仍未完成完整验收闭环，其中多项属于 P0：

| 测试项 | 模块 | 优先级 | 当前状态 | 风险 |
|---|---|---|---|---|
| TC-001 | 首页收益概览 | P0 | 未完成四方对账闭环 | 首页展示可能与资产/流水/后台不一致 |
| TC-003 | 我的资产 | P0 | 未完成资产口径逐项核账 | 可提现、冻结、累计口径可能不一致 |
| TC-008 | 广告中断不发积分 | P0 | 缺少真实小程序广告中断闭环 | 用户可能未完整看广告也获得积分 |
| TC-009 | 游戏失败不发积分 | P0 | 缺少真实失败链路闭环 | 失败也发积分属于 P0 风险 |
| TC-017 | D+2 自动兜底 | P0 | 缺少真实调度级闭环 | 缺数据时可能漏结算 |
| TC-018 | D+2 兜底幂等 | P0 | 缺少重复调度幂等验证 | 可能重复兜底发放 |
| TC-022 | 四方积分对账 | P0 | 缺少同账号同日期核账证据 | 前端、后台、流水、结算数据可能不一致 |
| TC-025 | 历史配置隔离 | P0 | 缺少历史数据抽样核账 | 配置变更可能污染历史结算 |
| TC-027 | 二级分销收益 | P0 | 缺少完整专项验证 | 二级收益可能漏发、错发、重复发 |
| TC-030 | 广告统计 | P0 | 有主路径脚本，但缺少真实广告日志核验 | 广告统计基础数据可能失真 |
| TC-034 | 后台审计 | P1 | 缺少运营动作审计日志核验 | 结算/补差追溯能力不足 |
| TC-039 | 文案展示 | P1 | 需要人工巡检确认 | 可能存在收益承诺或误导 |
| TC-040 | 历史数据迁移 | P1/P0 风险 | 缺少迁移前后抽样对比 | 历史账务可能异常 |

## 8. 发现的问题

### 问题 1：会员每日小游戏次数仍是旧口径

开发文档要求：

```text
普通用户 / 月卡 / 季卡 / 年卡 = 20 / 100 / 200 / 300
```

本轮抽查结果：

```text
D:\Desktop\vedo-project\myproject\services\game_task_service.py:26
daily_game_task_limit_normal = 10

D:\Desktop\vedo-project\myproject\services\game_task_service.py:27-29
月卡 / 季卡 / 年卡 = 100 / 150 / 200

D:\Desktop\vedo-project\myproject\services\config_service.py:22
VIP_PACKAGE_DAILY_LIMITS = {"month": 100, "quarter": 150, "year": 200}

D:\Desktop\vedo-project\myproject\scripts\verify_vip_entitlements.py:43-45
月卡 / 季卡 / 年卡脚本断言 = 100 / 150 / 200
```

结论：代码、配置默认值、自测脚本仍未统一到文档要求。这个问题不是“没有测到”，而是已经被直接测到了。

### 问题 2：P0 未全部闭环

当前 P0 未闭环项包括：

- 四方资产一致性。
- D+2 自动兜底结算。
- D+2 兜底幂等。
- 二级分销收益。
- 真实广告中断和游戏失败链路。
- 历史配置隔离。
- 历史数据迁移抽样。
- 广告真实日志与后台统计核验。

只要这些没有修复或没有补齐可复核证据，验收结论就不能给“通过”。

### 问题 3：v7 仍未执行 Bug 单修复

本轮遵循用户要求，没有直接扩大修改范围，没有修改核心业务代码。也就是说，本轮没有解决 v4/v5/v6 已经指出的业务阻断，所以结论不能改变。

## 9. 建议修复项

建议下一步不要再单纯生成 v8 自测报告，而是进入“按 Bug 单修复”阶段。否则继续复跑编译/构建，结论仍会是不通过。

| 修复项 | 优先级 | 建议动作 |
|---|---|---|
| 会员每日小游戏次数口径对齐 | P0 | 按 Bug 单只修改会员次数配置、接口默认值、脚本断言和必要展示口径 |
| 四方资产一致性闭环 | P0 | 补齐同账号同日期首页、资产、流水、后台结算核账脚本或受控验收记录 |
| D+2 自动兜底结算 | P0 | 补齐真实调度触发、状态标记、重复触发幂等验证 |
| 二级分销收益 | P0 | 补齐 A 邀 B、B 邀 C 的收益计算、流水、余额、幂等验证 |
| 真实广告失败链路 | P0 | 在微信开发者工具或真机验证广告中断、游戏失败不发积分 |
| 历史配置隔离和迁移抽样 | P0/P1 | 抽样验证配置变更不污染历史已结算数据，迁移前后总额一致 |

## 10. 需要人工确认部分

| 人工确认项 | 建议确认人 | 说明 |
|---|---|---|
| 会员每日小游戏次数最终口径 | 产品/业务负责人 | 当前文档为 `20 / 100 / 200 / 300`，代码未对齐 |
| 收益、会员、邀请相关文案 | 产品/运营/法务 | 避免固定收益、稳赚、保底等高风险表述 |
| 真实广告中断/失败链路 | 测试/产品/小程序负责人 | 需要微信开发者工具或真机环境确认 |
| 历史数据迁移抽样 | 技术负责人/测试负责人 | 需要选择真实或脱敏样本核账 |
| 是否进入上线确认 | 用户/最终负责人 | P0 未闭环前不建议进入 |

## 11. v7 自测结论

结论：不通过。

已通过的是工程构建和部分主路径专项验证；未通过的是 P0 业务闭环。当前如果继续只做“自测报告 + 构建复跑”，结论会继续不通过。

建议立即切换到开发修复任务：

1. 先修会员每日小游戏次数口径，因为这是最明确、最可定位、最容易闭环的 P0。
2. 再补 D+2 自动兜底、四方对账、二级分销收益三个 P0 闭环。
3. 修复后输出 Bug 修复报告，再交给 AI 测试官做复测与回归。
