# 网盘审核释放与失效处罚_验收报告

## 验收结论

需要人工确认

## P0 测试项覆盖情况

| P0 项 | 覆盖结果 | 说明 |
|---|---|---|
| 上传通过只释放一次 | 已覆盖 | 重复通过和 5 并发通过均只生成 1 条释放流水 |
| 上传拒绝扣回冻结奖励 | 已覆盖 | 冻结奖励扣回，总积分同步减少 |
| 上传确认失效处罚可用积分 | 已覆盖 | 已释放奖励从可用积分扣回 |
| 补链通过只释放一次 | 已覆盖 | 冻结积分转可用积分 |
| 补链拒绝扣回冻结奖励 | 已覆盖 | 冻结奖励扣回，总积分同步减少 |
| 补链确认失效处罚可用积分 | 已覆盖 | 已释放奖励从可用积分扣回，资源隐藏 |
| 投诉不奖励 | 已覆盖 | 3 次投诉账户余额无变化 |
| 3 次投诉自动隐藏资源 | 已覆盖 | 资源列表不再返回被投诉资源 |
| 幂等键唯一约束 | 已覆盖 | 新增迁移兜底，本地库已补约束并验证 |

## 已覆盖测试项

- `TC-001` 上传审核通过释放奖励。
- `TC-002` 上传拒绝扣回冻结奖励。
- `TC-003` 已释放上传确认失效处罚。
- `TC-004` 补链审核通过释放奖励。
- `TC-005` 补链拒绝扣回冻结奖励。
- `TC-006` 已释放补链确认失效处罚。
- `TC-007` 投诉不奖励。
- `TC-008` 三条投诉自动隐藏资源。
- `TC-009` 后台列表可查看。
- `TC-010` 积分流水唯一约束。
- `TC-011` 并发审核通过同一记录。

## 未覆盖测试项

- PC 后台页面未开发，因此未做页面级人工验收。
- 更高并发和跨进程压测未覆盖。
- 可用积分不足时的剩余追缴没有实现。
- 资源恢复上架、误投诉撤销未覆盖。

## 发现的问题

本轮发现并修复：

- 管理接口直接返回 `datetime` 导致响应 500，已统一使用 JSON 化返回。
- 本地数据库缺少 `points_ledger.idempotency_key` 唯一约束，已新增迁移并在本地补约束验证。

## Bug 修复任务单

暂无阻断性 Bug。

## 回归测试清单

- 上传/补链冻结奖励入账。
- 上传/补链审核释放。
- 上传/补链拒绝扣回。
- 上传/补链确认失效处罚。
- 投诉不奖励和自动隐藏。
- 资源解锁扣可用积分。
- 我的页冻结/可用积分展示。
- 积分流水查询。

## 上线前必须检查事项

- 正式环境执行 `012_ensure_points_ledger_idempotency_unique.py` 前，必须先确认 `points_ledger.idempotency_key` 无重复。
- PC 后台页面接入后，要重新验证列表筛选、操作确认弹窗、重复点击置灰。
- 如果业务要求处罚必须足额扣除，需要补“待追缴积分/账户风控”能力。

## 是否建议进入人工最终确认

建议进入接口级人工确认；不建议直接作为完整后台上线，因为 PC 后台页面尚未接入。
