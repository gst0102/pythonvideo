# 当前项目验收报告 v5

## 1. 验收结论

**结论：不通过**

本轮验收依据以下材料：

- 开发文档：`D:\Desktop\vedo-project\myproject\docs\yuexiang-stage2-docs\09-ecpm-settlement-rework.md`
- 测试清单：`D:\Desktop\vedo-project\myproject\docs\qa\当前项目_测试清单与验收标准v1.md`
- 开发自测报告：`D:\Desktop\vedo-project\myproject\docs\qa\当前项目_Codex自测报告v5.md`
- 上一轮验收报告：`D:\Desktop\vedo-project\myproject\docs\qa\当前项目_验收报告v4.md`

本轮 `v5` 自测报告明确说明：本轮未修改核心业务代码，仅补充自测归档、重新运行可复现构建/编译命令，并再次抽查会员次数口径。因此当前仍不能进入通过状态。

不通过原因：

1. **P0 测试项未全部覆盖**，四方对账、D+2 自动兜底、二级分销收益、真机广告中断/失败链路仍未闭环。
2. **会员每日小游戏次数默认值仍与开发文档不一致**，该问题已连续出现在 v3、v4、v5 自测与验收中，当前仍未修复。
3. **本轮新增证据主要是编译、构建和代码抽查**，不能替代积分、邀请、二级分销、广告统计、结算/返利类 P0 验收。

## 2. P0 测试项覆盖检查

**P0 测试项是否全部覆盖：否**

### 2.1 已覆盖的 P0 项

- TC-004 / TC-005：签到固定积分到账、重复签到拦截
- TC-007 / TC-010 / TC-011：小游戏广告完成后发放预估积分、同局幂等、防刷新重复发放
- TC-014 / TC-015 / TC-019 / TC-020：预估积分、D+1 结算、差额调整、补差幂等
- TC-021 / TC-035 / TC-036：积分流水类型、提现冻结/成功/退回主链路
- TC-026 / TC-028 / TC-029：邀请绑定、防自邀、防重复绑定
- TC-030：广告统计主链路有脚本覆盖，但仍需结合真实日志核验
- TC-037 / TC-038：会员支付权益、支付回调幂等

### 2.2 未覆盖或未闭环的 P0 项

- TC-001 / TC-003 / TC-022：首页、我的资产、积分流水、后台结算四方对账未完成真实账号逐项核验
- TC-008 / TC-009：真实小程序端“广告中断”“游戏失败”未完成真机闭环验证
- TC-017 / TC-018：D+2 自动兜底未完成真实调度级验证
- TC-025：后台配置变更后“只影响未来，不污染历史”未完成历史数据核账
- TC-027：二级分销收益未完成自动化或受控验收闭环

## 3. 未覆盖或异常项

### 3.1 未覆盖项

1. `四方账务一致性未闭环`
   - 影响用例：TC-001、TC-003、TC-022
   - 当前缺口：首页、资产页、积分流水、后台结算页缺少同一测试账号、同一日期口径逐项核对证据。

2. `D+2 自动兜底未闭环`
   - 影响用例：TC-017、TC-018
   - 当前缺口：缺少真实调度触发、执行一次、状态标记正确、重复触发仍幂等的完整验收记录。

3. `二级分销收益未闭环`
   - 影响用例：TC-027
   - 当前缺口：缺少二级链路收益计算、到账、幂等、流水一致性的专项验证证据。

4. `真机广告中断与失败链路未闭环`
   - 影响用例：TC-008、TC-009
   - 当前缺口：还没有来自微信开发者工具或真机环境的实际广告中断/失败验证记录。

5. `历史数据隔离未闭环`
   - 影响用例：TC-025、TC-040
   - 当前缺口：后台修改会员配置后是否污染历史已结算数据，仍缺少人工核账证据。

6. `后台审计未闭环`
   - 影响用例：TC-034
   - 当前缺口：缺少运营实际录入、结算、补差动作的审计日志核验记录。

7. `收益文案人工巡检未闭环`
   - 影响用例：TC-039
   - 当前缺口：首页、任务页、资产页、提现页、会员页等收益相关文案未逐页完成人工巡检。

### 3.2 异常项

1. `会员每日小游戏次数默认值仍与文档不一致`
   - 文档/测试清单要求：普通 / 月卡 / 季卡 / 年卡 = `20 / 100 / 200 / 300`
   - 当前独立抽查结果：
     - [game_task_service.py](D:/Desktop/vedo-project/myproject/services/game_task_service.py:26) 默认值仍为 `10 / 100 / 150 / 200`
     - [config_service.py](D:/Desktop/vedo-project/myproject/services/config_service.py:22) 会员默认值仍为 `100 / 150 / 200`
     - [verify_vip_entitlements.py](D:/Desktop/vedo-project/myproject/scripts/verify_vip_entitlements.py:43) 断言仍为 `100 / 150 / 200`
   - 结论：问题仍然存在，未见修复。

2. `旧阶段文档也存在旧口径残留`
   - `v5` 自测报告指出 `02-task-breakdown.md`、`04-acceptance.md`、`06-data-structure.md` 等旧阶段文档仍存在 `10 / 100 / 150 / 200` 或 `100 / 150 / 200` 口径痕迹。
   - 风险：团队执行时可能继续引用旧口径，导致开发、测试、运营配置不一致。

3. `v5 自测没有新增业务修复`
   - 本轮新增结果主要是：
     - `python -m compileall ...` 通过
     - `npm.cmd run build` 通过
     - `npm.cmd run type-check` 通过
     - `npm.cmd run build:mp-weixin` 通过
     - 会员次数口径抽查确认仍不一致
   - 结论：构建通过只能说明工程可出包，不能证明高风险业务链路通过验收。

## 4. 需要开发 Codex 修复的 Bug 单

本轮未发现新的独立业务缺陷，但上一轮 4 张 P0 Bug 单仍未关闭，继续作为开发修复输入。

### Bug 单 1

【Bug 标题】  
会员每日小游戏次数默认值与阶段二 v2 文档不一致

【严重级别】  
P0

【所属模块】  
会员权益 / 小游戏任务次数 / 配置中心 / 自测脚本 / 文档口径

【关联测试用例】  
TC-012、TC-024、TC-025、TC-037

【复现步骤】  
1. 对照 `09-ecpm-settlement-rework.md` 查看会员次数矩阵
2. 查看 `services/game_task_service.py`
3. 查看 `services/config_service.py`
4. 查看 `scripts/verify_vip_entitlements.py`
5. 抽查旧阶段文档中的会员次数口径

【实际结果】  
文档要求 `20 / 100 / 200 / 300`，当前代码、脚本及部分旧文档仍存在 `10 / 100 / 150 / 200` 或 `100 / 150 / 200`。

【预期结果】  
开发文档、业务代码、后台默认配置、接口返回、前端展示、自测脚本、测试文档应统一为同一套最终业务口径。

【可能原因】  
旧版会员权益默认值未同步升级，验证脚本和旧阶段文档也沿用旧口径。

【建议修复方向】  
1. 与产品/运营最终确认是否以 `20 / 100 / 200 / 300` 为准
2. 统一修改配置默认值、任务次数逻辑、脚本断言和展示字段
3. 清理或标注旧阶段文档中的旧口径，避免团队误用
4. 回归历史数据隔离，确保不污染已结算数据

【开发 Codex 执行指令】  
请只修复会员每日小游戏次数口径不一致相关逻辑，不要扩大修改范围。修复后补充验证“文档、默认配置、接口返回、前端展示、自测脚本”一致性，并输出修复报告。

【修复后必须回归】  
1. 普通 / 月卡 / 季卡 / 年卡次数上限  
2. 会员购买后权益即时生效  
3. 配置修改只影响未来数据  
4. 前端权益文案一致  
5. 自测脚本断言同步通过  
6. 旧文档口径不再误导开发和测试

【验收标准】  
同一环境下，文档、代码、接口、前端展示、自测脚本完全一致，且历史已结算数据不漂移。

### Bug 单 2

【Bug 标题】  
D+2 自动兜底结算缺少真实调度级验收闭环

【严重级别】  
P0

【所属模块】  
eCPM 日结 / 自动兜底调度 / 结算幂等

【关联测试用例】  
TC-017、TC-018

【复现步骤】  
1. 准备某测试日预估积分数据
2. 不录入该日真实广告数据
3. 到达 D+2 或模拟真实调度触发
4. 查看结算记录、流水、兜底标记和重复触发结果

【实际结果】  
当前仍缺少目标环境中的真实调度执行证据，无法确认 D+2 兜底一定触发且只触发一次。

【预期结果】  
系统应在 D+2 自动完成兜底结算，并具备严格幂等。

【可能原因】  
当前验证主要集中在 D+1 和补差脚本，缺少定时调度与状态标记的专项闭环。

【建议修复方向】  
1. 补充 D+2 自动兜底专项验证脚本或受控验收方案
2. 输出执行日志、状态标记、唯一结算键
3. 增加重复触发幂等断言

【开发 Codex 执行指令】  
请补充 D+2 自动兜底结算的自动化验证与必要日志/状态校验，不要修改无关业务。修复完成后提供可复跑的验证命令和结果说明。

【修复后必须回归】  
1. D+2 首次兜底执行  
2. D+2 重复触发幂等  
3. 兜底后真实数据补录差额调整  
4. 四方账务一致性

【验收标准】  
在受控测试日期下，D+2 自动兜底可真实触发、只执行一次、状态标记正确，且后续补录真实数据时只生成一次差额调整。

### Bug 单 3

【Bug 标题】  
四方资产一致性验收仍未形成目标环境闭环

【严重级别】  
P0

【所属模块】  
首页收益概览 / 我的资产 / 积分流水 / 后台结算 / 发布交付

【关联测试用例】  
TC-001、TC-003、TC-022

【复现步骤】  
1. 选择同一测试账号与测试日期
2. 分别查看首页、我的资产、积分流水、后台结算
3. 对比预估、已结算、调整、冻结、提现数据

【实际结果】  
当前报告仍缺少同一账号、同一日期下的逐项对账记录。

【预期结果】  
四方口径应可逐项核对且完全一致，并有可复核证据。

【可能原因】  
目标环境脚本、验收流程或人工核账步骤尚未补齐。

【建议修复方向】  
1. 补齐目标环境四方核账脚本与执行说明
2. 沉淀标准化核账输出
3. 将结果纳入自测报告

【开发 Codex 执行指令】  
请仅修复“四方一致性验收能力不足”问题，补齐目标环境可复跑方案与执行结果，不要扩大到无关业务代码。

【修复后必须回归】  
1. 首页收益摘要  
2. 我的资产页  
3. 积分流水  
4. 后台结算页  
5. 提现冻结/成功/差额调整联动

【验收标准】  
目标环境中能完成同账号、同日期四方口径对账，结果一致且可审阅。

### Bug 单 4

【Bug 标题】  
二级分销收益缺少验证闭环

【严重级别】  
P0

【所属模块】  
邀请奖励 / 二级分销 / 结算后收益分发

【关联测试用例】  
TC-027

【复现步骤】  
1. 构造 A 邀请 B，B 邀请 C 的二级关系
2. 让 C 完成有效任务并完成结算
3. 查看 A、B 收益到账与流水
4. 重复请求或重复结算验证幂等

【实际结果】  
当前仍缺少二级分销专项自动化或人工验收结果。

【预期结果】  
二级分销收益应按规则准确到账，不漏发、不重复、不串级。

【可能原因】  
现有验证更多覆盖一级邀请绑定和支付/广告主链路，未深入二级收益分发。

【建议修复方向】  
1. 补充二级分销专项验证脚本或受控验收步骤
2. 校验与正式结算、差额调整的联动
3. 增加收益幂等断言

【开发 Codex 执行指令】  
请补齐二级分销收益的验证闭环与必要防重校验，只处理与二级收益相关的逻辑和测试，不要扩大修改范围。

【修复后必须回归】  
1. 一级邀请收益  
2. 二级分销收益  
3. 重复结算 / 重复请求幂等  
4. 流水与余额一致性

【验收标准】  
二级分销收益在正常、重复请求、补差调整场景下均计算正确、仅发放一次、账务可追溯。

## 5. 回归测试清单

修复后建议至少执行以下回归：

1. 首页收益概览：今日预估、昨日已结算、累计积分、已提现金额展示正确。
2. 我的资产：累计、可提现、冻结、已提现口径一致。
3. 签到主链路：首次签到、重复签到、连签奖励正常。
4. 小游戏主链路：成功完成 + 广告完成后发预估积分。
5. 小游戏异常链路：广告中断、游戏失败、刷新页面、重复点击不发或不重复发积分。
6. D+1 真实 eCPM 结算：真实数据录入后结算正确。
7. D+2 自动兜底：自动触发、只执行一次、标记正确。
8. 真实数据后补：只生成一次差额调整，不覆盖历史。
9. 会员档位：普通 / 月卡 / 季卡 / 年卡次数和系数正确。
10. 配置修改：只影响未来任务和未来结算，不污染历史。
11. 一级邀请奖励：绑定正确、收益正确、幂等正确。
12. 二级分销收益：收益计算正确、幂等正确、流水正确。
13. 广告统计：曝光、完成、点击、广告位维度统计准确。
14. 提现链路：仅已结算积分可提现，冻结/成功/驳回口径正确。
15. 支付链路：支付成功生效、支付失败不生效、重复回调不重复生效。
16. 四方对账：首页、资产、流水、后台结算结果一致。
17. 文案巡检：明确“预估积分次日结算”，无夸大收益承诺。
18. 后台审计：录入、结算、补差动作可追踪。
19. 文档口径回归：新旧文档中会员次数口径不再互相冲突。

## 6. 上线前必须检查的事项

1. 开发文档、测试清单、自测报告、验收报告全部归档齐全。
2. 所有 P0 项都有执行证据，不能只停留在“构建通过”或“脚本存在”。
3. 会员每日次数最终业务口径已确认，并在代码、配置、接口、前端展示、自测脚本、文档中完全一致。
4. 已在受控测试日期完成一次 D+1 真实结算验证。
5. 已在受控测试日期完成一次 D+2 自动兜底验证。
6. 已完成一次“兜底后补真实数据”的差额调整验证。
7. 已完成首页 / 资产 / 流水 / 后台结算四方对账。
8. 已验证预估积分不可提现，提现仅基于已结算积分。
9. 已验证一级邀请奖励、二级分销收益在新模型下正确。
10. 已验证广告曝光、完成、点击、eCPM、收入统计的结算基础数据可信。
11. 已验证支付成功、支付失败、重复支付回调场景。
12. 已验证后台录入、结算、补差操作审计日志完整可追踪。
13. 已完成真机或微信开发者工具下的广告中断 / 游戏失败链路验证。
14. 已人工巡检首页、任务页、资产页、提现页、会员页收益文案，无“预估即实际到账”误导。
15. 完成最终人工业务确认后，方可进入上线审批。

## 7. 是否建议进入人工最终确认

**当前不建议进入最终上线确认。**

`v5` 自测报告再次确认当前没有业务修复，且会员次数口径不一致、四方对账、D+2 兜底、二级分销收益等 P0 问题仍未闭环。建议继续按本报告中的 4 张 P0 Bug 单推进开发修复，修复完成后再进入复测与回归。

