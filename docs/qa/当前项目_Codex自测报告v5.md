# 当前项目 Codex 自测报告 v5

生成时间：2026-06-07

## 1. 本次修改目标

本轮未修改核心业务代码，仅根据以下材料补充一版 `v5` 自测报告，作为开发侧对当前阻断状态的再次归档：

- `D:\Desktop\vedo-project\AGENTS.md`
- `D:\Desktop\vedo-project\myproject\docs\yuexiang-stage2-docs\09-ecpm-settlement-rework.md`
- `D:\Desktop\vedo-project\myproject\docs\qa\当前项目_测试清单与验收标准v1.md`
- `D:\Desktop\vedo-project\myproject\docs\qa\当前项目_验收报告v4.md`
- `D:\Desktop\vedo-project\myproject\docs\qa\当前项目_Codex自测报告v4.md`

本轮目标：

- 按测试清单继续核对当前是否满足 P0 通过条件
- 补跑现有自动化命令
- 对会员每日小游戏次数口径做代码级抽查
- 统一沉淀“已通过项、未覆盖项、发现的问题、建议修复项、需要人工确认部分”

说明：

- 用户给出的保存路径写为 `当前项目_Codex自测报告v5md`，本次按项目既有命名习惯保存为 `当前项目_Codex自测报告v5.md`。
- 本轮仍是“开发侧自测归档”，不是业务修复提交。

## 2. 修改文件列表

- `D:\Desktop\vedo-project\myproject\docs\qa\当前项目_Codex自测报告v5.md`

## 3. 核心实现说明

- 未修改后端、前端、脚本、配置或数据库迁移逻辑。
- 重新执行了当前仓库中可直接复跑的自动化命令：
  - 后端静态编译
  - PC 后台生产构建
  - 小程序类型检查
  - 小程序微信构建
- 额外对会员次数口径做了代码级抽查，确认当前代码和脚本仍未对齐文档要求。
- 结合 `验收报告 v4` 再次确认：当前仍应维持“不通过”结论，且继续沿用 4 张 P0 Bug 单推进修复。

## 4. 已运行测试

| 类型 | 命令 | 结果 |
|---|---|---|
| 后端静态编译 | `python -m compileall controllers services schemas models scripts -q` | 通过 |
| PC 后台构建 | `npm.cmd run build` | 通过 |
| 小程序类型检查 | `npm.cmd run type-check` | 通过 |
| 小程序微信构建 | `npm.cmd run build:mp-weixin` | 通过 |
| 会员次数口径抽查 | `rg -n ... game_task_service.py / config_service.py / verify_vip_entitlements.py` | 发现口径仍不一致 |

补充说明：

- `adminVideo` 构建仍有 Rollup 大包告警和 `#__PURE__` 注释告警，但不影响构建通过。
- `video-ts` 微信构建仍有 Sass legacy API deprecation warning，但不影响构建通过。
- 生产容器内结算、提现、签到、小游戏广告、登录邀请、会员权益专项脚本，本轮未重新执行，沿用 `v3/v4` 报告中已有记录。

## 5. 已通过测试项

### 5.1 本轮重新验证通过

- 后端静态编译通过，当前 Python 代码未出现新增语法阻断。
- PC 后台生产构建通过，后台前端工程可正常出包。
- 小程序类型检查通过，当前前端类型检查未出现新增阻断错误。
- 小程序微信构建通过，当前 `mp-weixin` 产物可正常生成。

### 5.2 沿用前序自测与验收材料的已通过项

- TC-004 / TC-005：签到固定积分到账、重复签到拦截已覆盖。
- TC-007 / TC-010 / TC-011：小游戏广告完成后发放预计积分、同回合幂等、防刷新重复发放已覆盖。
- TC-014 / TC-015 / TC-019 / TC-020：预计积分、D+1 结算、差额调整、补差幂等已覆盖。
- TC-021 / TC-035 / TC-036：提现冻结、成功、驳回、流水类型已覆盖。
- TC-026 / TC-028 / TC-029：邀请绑定、防自邀、防重复绑定已覆盖。
- TC-030：广告统计主链路有脚本覆盖，但仍需真实日志核验。
- TC-037 / TC-038：会员支付后权益生效、重复回调幂等已覆盖。

## 6. 未覆盖测试项

以下项目当前仍未形成闭环证据，且 P0 项优先级最高：

- TC-001 / TC-003 / TC-022：首页、我的资产、积分流水、后台结算四方对账未完成真实账号逐项核验。
- TC-008 / TC-009：真实小程序端“广告中断”“游戏失败”场景未完成真机闭环验证。
- TC-017 / TC-018：D+2 自动兜底未完成真实调度级验证，缺少“真实触发且只触发一次”的证据。
- TC-025：后台改会员配置后“只影响未来、不污染历史”未完成人工历史数据核对。
- TC-027：二级分销收益未完成自动化或受控验收闭环。
- TC-034：后台录入、结算、补差动作的审计日志未完成闭环验证。
- TC-039：收益相关前端文案未逐页完成人工巡检。
- TC-040：历史旧积分数据迁移后的抽样核对未覆盖。

## 7. 发现的问题

### 7.1 阻断问题

1. P0 仍未全部闭环。
   - 关键缺口仍集中在四方对账、D+2 自动兜底、二级分销收益。
   - 依据 `AGENTS.md` 和测试清单，只要 P0 未全部覆盖，就不能给出“通过”。

2. 会员每日小游戏次数默认值仍与文档不一致。
   - 文档要求：`20 / 100 / 200 / 300`
   - 本轮代码级抽查结果：
     - [game_task_service.py](D:/Desktop/vedo-project/myproject/services/game_task_service.py) 第 `26-29` 行仍为 `10 / 100 / 150 / 200`
     - [config_service.py](D:/Desktop/vedo-project/myproject/services/config_service.py) 第 `22` 行与 `47/59/71/226/238/250` 行仍指向会员默认值 `100 / 150 / 200`
     - [verify_vip_entitlements.py](D:/Desktop/vedo-project/myproject/scripts/verify_vip_entitlements.py) 第 `43-45` 行断言仍为 `100 / 150 / 200`
   - 该问题与 `验收报告 v4` 结论一致，当前应继续视为未修复。

### 7.2 非阻断但需记录的问题

1. `adminVideo` 构建仍有大包告警。
2. `video-ts` 微信构建仍有 Sass legacy API 弃用告警。
3. `02-task-breakdown.md`、`04-acceptance.md`、`06-data-structure.md` 等旧阶段文档中仍存在 `10 / 100 / 150 / 200` 或 `100 / 150 / 200` 口径痕迹，说明项目内部文档口径也未完全收敛。

## 8. 建议修复项

本轮不直接修改业务代码。后续修复建议继续严格按 `当前项目_验收报告v4.md` 中的 4 张 P0 Bug 单执行：

1. Bug 单 1：修复会员每日小游戏次数口径不一致。
   - 只修改与会员默认权益、任务次数限制、验证脚本、必要展示口径直接相关的逻辑。

2. Bug 单 2：补齐 D+2 自动兜底真实调度级闭环验证。
   - 目标是拿到“真实触发、只执行一次、补录真实数据后只生成一次差额调整”的证据。

3. Bug 单 3：补齐四方资产一致性验收能力。
   - 目标是让目标环境能对同账号同日期完成首页 / 资产 / 流水 / 后台结算逐项核对。

4. Bug 单 4：补齐二级分销收益验证闭环。
   - 至少覆盖正常发放、重复请求幂等、差额调整联动、流水一致性。

## 9. 可能影响范围

- 会员权益配置
- 小游戏每日任务次数限制
- eCPM 日结与 D+2 自动兜底链路
- 邀请奖励 / 二级分销收益
- 首页、资产页、流水页、后台结算页的一致性口径

## 10. 需要 AI 测试官复核的事项

- 是否继续直接沿用 `当前项目_验收报告v4.md` 中的 4 张 P0 Bug 单进入开发修复。
- 会员每日小游戏次数最终业务口径是否明确以 `20 / 100 / 200 / 300` 为准。
- D+2 自动兜底是否能在受控测试日期完成一次真实调度验收。
- 四方对账是否能在目标环境完成同账号同日期核验留档。
- 二级分销收益和真机广告中断场景是否需要补充专项验收记录。

## 11. 需要人工确认部分

- 真实后台账号下的结算录入、执行、审计日志闭环。
- 微信开发者工具或真机环境下的广告完成 / 中断 / 失败路径。
- 会员权益最终业务口径是以新版文档为准，还是需要反向修正文档群。

## 12. 自测结论

结论：**不通过**

原因如下：

- 本轮重新执行的自动化命令全部通过，但这些结果只说明当前代码仍可编译、构建，不代表高风险业务链路已验收通过。
- 按测试清单和项目规则，P0 仍未全部闭环，尤其是四方对账、D+2 自动兜底、二级分销收益。
- 会员每日小游戏次数默认值仍未对齐文档要求，且本轮代码抽查再次证实问题仍然存在。

因此，当前版本仍不建议进入最终上线确认。后续应按 `当前项目_验收报告v4.md` 中的 P0 Bug 单逐项修复，再进入复测和回归。
