# 猜拳小游戏奖励规则_验收报告

## 验收结论

需要人工确认

原因：代码层验证已通过，但尚未在微信开发者工具里完成“赢一局看广告到账 4 分、输一局扣 2 分、平局不加不扣”的手工验收。

## P0 覆盖情况

| 测试项 | 覆盖情况 | 结果 |
|---|---|---|
| 后端根据 `user_choice` 判定输赢 | 代码检查覆盖 | 通过 |
| 赢局奖励 4 分 | 规则断言已加入验证脚本 | 通过 |
| 输局扣 2 分、平局 0 分 | 规则断言已加入验证脚本 | 通过 |
| 前端不再提交 `result` 决定输赢 | TypeScript 检查覆盖 | 通过 |
| 重复广告奖励不重复发分 | 沿用并更新 `verify_game_ad_flow.py` | 待真实库执行 |

## 已覆盖测试项

- `python3 -m py_compile schemas/game.py controllers/game.py services/game_task_service.py scripts/verify_game_ad_flow.py`
- `PATH=".../node/bin:$PATH" ./node_modules/.bin/vue-tsc --noEmit`

## 未覆盖测试项

- 未执行真实数据库版 `scripts/verify_game_ad_flow.py --execute`。
- 未在微信开发者工具手工验证广告领取后积分到账。

## 发现的问题

暂无阻断问题。

## 回归测试清单

- 猜拳赢局：后端返回电脑出拳、结果为赢、预计积分 4。
- 猜拳输局：预计积分 -2，不打开广告，积分明细新增 `game_penalty`。
- 猜拳平局：预计积分 0，不打开广告。
- 赢局看完广告：积分明细新增 4 分 `game_estimated`。
- 同一局重复领取：不重复加分。
