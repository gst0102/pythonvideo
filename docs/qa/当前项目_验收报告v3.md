# 当前项目验收报告 v3

## 1. 验收结论

**结论：不通过**

本轮基于以下材料完成验收判断：

- 开发文档：`D:\Desktop\vedo-project\myproject\docs\yuexiang-stage2-docs\09-ecpm-settlement-rework.md`
- 测试清单：`D:\Desktop\vedo-project\myproject\docs\qa\当前项目_测试清单与验收标准v1.md`
- 开发自测报告：`D:\Desktop\vedo-project\myproject\docs\qa\当前项目_Codex自测报告v3.md`

判定为“不通过”的主要原因：

1. **P0 测试项未全部覆盖**，当前仍缺少四方对账、D+2 自动兜底真执行、二级分销收益、真实前端广告中断等关键验收闭环。
2. **已发现明确业务口径异常**：当前代码中的会员每日小游戏次数默认值，与本轮开发文档和测试清单不一致。
3. **上线前关键验收能力不完整**：四方资产一致性脚本未进入当前生产镜像，导致无法在目标环境快速复核首页 / 我的资产 / 流水 / 结算页一致性。

## 2. P0 覆盖情况结论

**P0 测试项是否全部覆盖：否**

### 2.1 已覆盖的 P0 项

- TC-004 / TC-005：签到固定积分到账、重复签到拦截
- TC-007 / TC-010 / TC-011：小游戏广告完成后预估积分发放、同局幂等、防刷新重复发放
- TC-014 / TC-015 / TC-019 / TC-020：预估积分、D+1 结算、差额调整、补差幂等
- TC-021 / TC-035 / TC-036：积分流水类型、提现冻结/成功/退回主链路
- TC-026 / TC-028 / TC-029：邀请绑定、防自邀、防重复绑定
- TC-030 / TC-031：广告位轮换、广告事件幂等、广告位维度统计
- TC-037 / TC-038：会员支付权益、支付回调幂等

### 2.2 未完全覆盖的 P0 项

- TC-001 / TC-003 / TC-022：首页、我的资产、流水、后台结算四方对账未用真实账号做逐项人工核验
- TC-008 / TC-009：真实小程序端“广告中断”“游戏失败”未做真机闭环验证
- TC-017 / TC-018：D+2 自动兜底结算未做真实定时任务触发验证
- TC-025：后台修改会员配置后“只影响未来，不污染历史”未做人工历史数据核账
- TC-027：二级分销收益未纳入本轮自动化脚本覆盖

## 3. 未覆盖或异常项

### 3.1 未覆盖项

1. `四方资产一致性未闭环`
   - 影响用例：TC-001、TC-003、TC-022
   - 风险：前端展示与后台结算、流水、账户快照存在口径不一致时，当前证据不足以拦截。

2. `D+2 自动兜底未做真实调度验证`
   - 影响用例：TC-017、TC-018
   - 风险：兜底结算可能不触发、重复触发、或状态标记错误。

3. `二级分销收益未闭环`
   - 影响用例：TC-027
   - 风险：收益可能漏发、重复发、串级发放，属于高风险账务问题。

4. `真机广告中断与失败链路未闭环`
   - 影响用例：TC-008、TC-009
   - 风险：用户可能通过中断广告、失败重试等方式获取不应得到的预估积分。

5. `历史数据隔离未闭环`
   - 影响用例：TC-025、TC-040
   - 风险：后台改配置后可能污染历史已结算数据，造成账务漂移。

### 3.2 异常项

1. `会员每日小游戏次数默认值与文档不一致`
   - 文档/测试清单期望：普通 / 月卡 / 季卡 / 年卡 = `20 / 100 / 200 / 300`
   - 当前代码核实结果：
     - `D:\Desktop\vedo-project\myproject\services\game_task_service.py` 默认值为 `10 / 100 / 150 / 200`
     - `D:\Desktop\vedo-project\myproject\services\config_service.py` 默认值为 `100 / 150 / 200`
     - `D:\Desktop\vedo-project\myproject\scripts\verify_vip_entitlements.py` 断言值为 `100 / 150 / 200`
   - 结论：当前实现、自测脚本、开发文档三者未对齐，属于明确业务口径异常。

2. `生产镜像缺少四方资产一致性校验脚本`
   - 本地脚本存在：`D:\Desktop\vedo-project\myproject\scripts\verify_points_asset_consistency.py`
   - 自测报告说明：当前生产容器中缺少该脚本
   - 风险：上线前难以快速在目标环境完成首页 / 资产 / 流水 / 结算页一致性复核。

3. `生产环境未执行结算写入型人工验收`
   - 当前只有 `GET` 级健康检查和页面可达性检查
   - 未执行受控测试日期、测试账号下的真实录入与结算按钮闭环
   - 风险：后台录入、触发、审计、结果展示仍可能存在仅在真实操作时出现的问题。

## 4. 需要开发 Codex 修复的 Bug 单

### Bug 单 1

【Bug 标题】  
会员每日小游戏次数默认值与阶段二 v2 文档不一致

【严重级别】  
P0

【所属模块】  
会员权益 / 小游戏任务次数 / 配置中心 / 自测脚本

【关联测试用例】  
TC-012、TC-024、TC-025、TC-037

【复现步骤】  
1. 阅读开发文档 `09-ecpm-settlement-rework.md` 与测试清单 `当前项目_测试清单与验收标准v1.md`
2. 查看 `services/game_task_service.py` 中小游戏每日次数默认值
3. 查看 `services/config_service.py` 中会员包每日次数默认值
4. 查看 `scripts/verify_vip_entitlements.py` 中会员次数断言

【实际结果】  
当前代码/脚本口径为普通 10、月卡 100、季卡 150、年卡 200，和文档要求的 20、100、200、300 不一致。

【预期结果】  
开发文档、实际代码、后台默认配置、自测脚本、前端权益展示应统一为同一套业务口径。

【可能原因】  
旧版会员权益默认值未随阶段二 v2 文档同步，自测脚本也沿用了旧断言值。

【建议修复方向】  
1. 与产品/运营确认最终口径是否采用 `20 / 100 / 200 / 300`
2. 统一修正 `config_service.py`、`game_task_service.py`、相关初始化逻辑和展示接口
3. 同步修正 `verify_vip_entitlements.py`
4. 回归会员购买、游戏次数限制、历史配置生效范围

【开发 Codex 执行指令】  
请只修复会员每日小游戏次数口径不一致相关逻辑，不要扩大修改范围。修复后补充验证“默认配置、接口返回、前端展示、自测脚本”四处一致性，并输出修复报告。

【修复后必须回归】  
1. 普通 / 月卡 / 季卡 / 年卡次数上限  
2. 会员购买后权益即时生效  
3. 配置修改只影响未来数据  
4. 前端任务页权益文案一致  
5. 自测脚本断言同步通过

【验收标准】  
同一环境下，文档、代码默认值、后台接口返回、前端展示、自测脚本断言完全一致，且不会污染历史已结算数据。

### Bug 单 2

【Bug 标题】  
D+2 自动兜底结算缺少真实调度级验收闭环

【严重级别】  
P0

【所属模块】  
eCPM 日结 / 自动兜底调度 / 结算幂等

【关联测试用例】  
TC-017、TC-018

【复现步骤】  
1. 准备某测试日的小游戏预估积分数据
2. 不录入该日真实广告数据
3. 到达 D+2 结算时点或模拟真实调度触发
4. 查看结算记录、积分流水、兜底标记、重复触发结果

【实际结果】  
当前只有自测报告结论，没有目标环境中的真实调度执行证据，无法确认 D+2 兜底一定触发且只触发一次。

【预期结果】  
系统应在 D+2 自动完成兜底结算，并且无论任务重试多少次，都只允许生成一次有效兜底结果。

【可能原因】  
当前验证更多集中在 D+1 与补差脚本，缺少对定时调度、状态标记、重复执行保护的独立闭环。

【建议修复方向】  
1. 补充 D+2 自动兜底专项验证脚本
2. 输出兜底标记、唯一结算键、重复执行保护日志
3. 增加调度任务级回归用例

【开发 Codex 执行指令】  
请补充 D+2 自动兜底结算的自动化验证与必要日志/状态校验，不要修改无关业务。修复完成后提供可复跑的验证命令和结果说明。

【修复后必须回归】  
1. D+2 自动兜底首次执行  
2. D+2 重复触发幂等  
3. 兜底后真实数据补录差额调整  
4. 首页/资产/流水/后台结算口径一致

【验收标准】  
在受控测试日期下，D+2 自动兜底能真实触发、只执行一次、状态标记正确，且后续补录真实数据时只生成一次差额调整。

### Bug 单 3

【Bug 标题】  
四方资产一致性验收能力不完整，生产镜像缺少统一核账脚本

【严重级别】  
P0

【所属模块】  
首页收益概览 / 我的资产 / 积分流水 / 后台结算 / 发布交付

【关联测试用例】  
TC-001、TC-003、TC-022

【复现步骤】  
1. 在本地仓库确认存在 `scripts/verify_points_asset_consistency.py`
2. 对照自测报告检查生产镜像内容
3. 尝试在目标环境执行四方一致性校验

【实际结果】  
本地有脚本，但当前生产容器缺少该脚本，导致无法在目标环境一键复核四方资产一致性。

【预期结果】  
上线候选镜像应包含与本次改造相匹配的关键核账脚本，便于上线前与回归阶段快速复核。

【可能原因】  
镜像构建或发布物未同步包含新增验证脚本，或发布流程未将 QA 验收脚本纳入交付清单。

【建议修复方向】  
1. 将 `verify_points_asset_consistency.py` 纳入镜像发布物
2. 补充使用说明和执行命令
3. 在自测报告中加入脚本执行结果

【开发 Codex 执行指令】  
请仅修复“验收脚本未进入交付物”问题，补齐发布物与使用说明，并输出验证结果。不要扩大到无关业务代码改动。

【修复后必须回归】  
1. 目标环境脚本可执行  
2. 首页/资产/流水/结算四方一致性核账  
3. 与提现、差额调整、邀请返利联动数据检查

【验收标准】  
目标环境中可直接执行四方一致性脚本，并输出可审阅结果，能够支持上线前核账验收。

### Bug 单 4

【Bug 标题】  
二级分销收益缺少自动化与验收闭环

【严重级别】  
P0

【所属模块】  
邀请奖励 / 二级分销 / 结算后收益分发

【关联测试用例】  
TC-027

【复现步骤】  
1. 构造 A 邀请 B，B 邀请 C 的二级关系
2. 让 C 完成小游戏并完成结算
3. 查看 B、A 的收益分发与流水
4. 重复请求或重复结算验证幂等

【实际结果】  
当前自测报告未给出二级分销专项脚本或人工验收结果，无法确认收益计算、发放时点、幂等性是否正确。

【预期结果】  
二级分销收益应按既定规则准确到账，不漏发、不重复、不串级。

【可能原因】  
当前测试覆盖聚焦登录邀请绑定和一级关系，未向二级收益链路延伸。

【建议修复方向】  
1. 补充二级分销专项验证脚本或受控验收步骤
2. 校验与正式结算、差额调整、退款/撤销场景的联动
3. 增加收益幂等断言

【开发 Codex 执行指令】  
请补齐二级分销收益的验证闭环与必要防重校验，只处理与二级收益相关的逻辑和测试，不要扩大修改范围。

【修复后必须回归】  
1. 一级邀请收益  
2. 二级分销收益  
3. 重复结算/重复回调幂等  
4. 流水与余额一致性

【验收标准】  
二级分销收益在正常、重复请求、补差调整场景下都能保持计算正确、发放一次、账务可追溯。

## 5. 回归测试清单

修复后至少执行以下回归：

1. 首页收益概览：今日预估、昨日已结算、累计积分、已提现金额展示正确。
2. 我的资产：累计、可提现、冻结、已提现口径一致。
3. 签到链路：首次签到、重复签到、连签奖励正常。
4. 小游戏主链路：成功完成 + 广告完成后发预估积分。
5. 小游戏异常链路：广告中断、游戏失败、刷新页面、重复点击不发或不重复发积分。
6. D+1 真实 eCPM 结算：真实数据录入后结算结果正确。
7. D+2 自动兜底：自动触发、只执行一次、标记正确。
8. 真实数据后补：只生成一次差额调整，不覆盖历史展示。
9. 会员档位：普通 / 月卡 / 季卡 / 年卡次数和系数正确。
10. 后台配置变更：仅影响未来任务与未来结算，不污染历史。
11. 一级邀请奖励：绑定正确、收益正确、幂等正确。
12. 二级分销收益：收益计算正确、幂等正确、流水正确。
13. 广告统计：曝光、完成、点击、广告位维度统计准确。
14. 提现链路：仅已结算积分可提现，冻结/成功/驳回口径正确。
15. 会员支付：支付成功生效、支付失败不生效、重复回调不重复生效。
16. 四方对账：首页、我的资产、积分流水、后台结算结果一致。
17. 文案巡检：明确“预估积分次日结算”，无夸大收益承诺。

## 6. 上线前必须检查的事项

1. 开发文档、测试清单、自测报告、验收报告全部归档。
2. 所有 P0 项都有执行证据；不能只停留在“脚本存在”或“页面可打开”。
3. 会员每日次数和权益口径已与产品/运营最终确认，并在代码、配置、前端展示、自测脚本中完全一致。
4. 已在受控测试日期完成一次 D+1 真实结算验证。
5. 已在受控测试日期完成一次 D+2 自动兜底验证。
6. 已完成一次“兜底后补真实数据”的差额调整验证。
7. 已完成首页 / 资产 / 流水 / 后台结算四方对账。
8. 已验证预估积分不可提现，提现仅使用已结算积分。
9. 已验证一级邀请奖励、二级分销收益在新结算模型下正确。
10. 已验证广告曝光、完成、点击、eCPM、收入的结算基础数据可信。
11. 已验证支付成功、支付失败、重复支付回调场景。
12. 已确认目标环境包含关键验收脚本与可复跑命令。
13. 已人工巡检首页、任务页、资产页、提现页、会员页文案，确保不误导用户“预估=实际到账”。
14. 已确认后台审计日志可追踪录入、结算、补差动作。
15. 完成最终人工业务确认后，方可进入上线审批。

## 7. 是否建议进入人工最终确认

**当前不建议进入最终上线确认。**

建议先完成以下动作后再发起复测：

1. 修复会员每日次数口径不一致问题。
2. 补齐 D+2 自动兜底真实验证闭环。
3. 补齐二级分销收益验证闭环。
4. 补齐四方资产一致性目标环境验收能力。

