# 操作记录

## 2026-06-02

- 查看本地项目目录、文件列表与 Git 状态，确认当前分支为 `master`，本地工作区无未提交变更。
- 阅读 `DEPLOY.md`、`docker-compose.yml`、`Dockerfile`、`pyproject.toml`、`.env.example`，确认部署目标为 `81.70.84.35`，项目路径文档建议为 `/opt/video-service`。
- 本地尝试执行 `docker compose config`，发现本机未安装或未暴露 `docker` 命令。
- 直接使用默认 SSH 密钥连接 `root@81.70.84.35`、`ubuntu@81.70.84.35` 均失败，提示 `Permission denied (publickey)`。
- 检查 MobaXterm 配置，发现服务器会话 `81.70.84.35` 与密钥 `E:\Program Files\MobaXterm_Portable_v26.3\videoKey.pem`。
- 使用 MobaXterm 目录内私钥直接 SSH 时，OpenSSH 因私钥 ACL 权限过宽拒绝加载，后续准备复制到用户 `.ssh` 目录并收紧权限。
- 找到 MobaXterm 实际会话密钥 `D:\Desktop\vedo-project\vidoekey.pem`，复制到当前用户 `.ssh\vidoekey.pem` 并收紧权限后，成功通过 `ubuntu@81.70.84.35` 登录服务器。
- 确认服务器实际项目目录为 `/opt/pythonvideo`，不是部署文档中的 `/opt/video-service`；服务器容器状态显示 `video-service-app` 持续重启，PostgreSQL 与 Redis 均健康。
- 查看服务器应用日志，发现重复报错 `exec /app/.venv/bin/uvicorn: no such file or directory`。
- 检查服务器镜像内部文件，确认 `/app/.venv/bin/uvicorn` 存在，但脚本首行为 `#!/build/.venv/bin/python`，运行镜像中不存在该路径，因此启动失败。
- 本地修改 `Dockerfile`：builder 阶段从 `/build` 改为 `/app`，并同步调整 `uvicorn --version` 验证路径与最终阶段 `.venv` 复制路径，确保 console script shebang 指向运行镜像中的 `/app/.venv/bin/python`。
